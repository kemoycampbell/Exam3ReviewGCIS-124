package practicum3.slidingpuzzle;

/**
 * Used to indicate the direction from which a move is being made on a sliding
 * puzzle. For example, LEFT indicates that the tile to the left of the empty
 * space on the board is being moved into the empty space.
 */
public enum Direction {
    ABOVE,
    BELOW,
    LEFT,
    RIGHT
}
